{
  "version": 2,
  "builds": [
    {
      "src": "chfrontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/chfrontend/build/$1"
    }
  ]
}
